div
	div.addr-container(ng-mousedown="$event.preventDefault()",ng-class="name",ng-switch="addressOrCoords",ng-swipe-left="addressOrCoords=addressOrCoords==='coords'?'addr':'coords'")
		div.addr(ng-switch-when="coords") TBD
		div.addr(ng-switch-default) {{address.address}}
div.coords {{address.dms}}
